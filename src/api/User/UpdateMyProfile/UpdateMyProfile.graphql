type UpdateMyProfileResponse {
    ok: Boolean!
    error: String
}

type Mutation {
    UpdateMyProfile(
        firstname: String
        lastname: String
        email: String
        password: String
        profilePhoto: String
        age: Int
    ): UpdateMyProfileResponse!
}